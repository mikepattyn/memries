{
  "name": "memries",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test --filter=!@memries/e2e",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
